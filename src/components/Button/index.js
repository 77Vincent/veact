import Veact from '../../service/veact'

import './index.scss'

export default ({ children }) => {
  return (
    <div className="App-button" >
      <div>
        {children}
        bbbbbbbbb
      </div>
    </div>
  ) 
}
