import Veact from '../../service/veact'

import './index.scss'

export default () => {
  return (
    <header className="App-header" >
      <h1>Welcome to the Veact App</h1> 
      <h4>Pure functional, simplified React-like UI library.</h4>
    </header>
  )
}
