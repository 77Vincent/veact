import Veact from '../../service/veact'

export default () => {
  return (
    <header
      className="App-header"
      style={{
        textAlign: 'center',
        padding: '10px',
      }}
    >
      <h1>Welcome to the Veact App</h1> 
    </header>
  )
}
