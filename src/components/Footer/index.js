import Veact from '../../service/veact'

export default () => {
  return (
    <footer
      className="App-footer"
      style={{
        textAlign: 'center',
        color: 'white',
        backgroundColor: 'gray',
      }}
    >
      Footer
    </footer>
  ) 
}